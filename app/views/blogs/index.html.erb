<div class ="jumbotron text-center"style ="height:300px; color:white; text-shadow: 2px 2px 4px #666; 
 background:url(/assets/about-bg.jpg);  background-size:cover;">
  <h1>ブログ一覧</h1>
  <p>ブログ機能のお試しアプリです</p>
</div>


<table class ="table h4 ">
  <tr><th>タイトル</th><th>内容</th></tr>

<% @blogs.each do |blog| %>
  <tr>
    <td><%= blog.title %></td><td><%= blog.content %></td>

    <td>
      <div class="btn btn-default text-muted">
        <%= link_to "ブログを編集する", edit_blog_path(blog.id) %>
      </div>
    </td>
    <td>
      <button class="btn btn-default">
        <%= link_to 'ブログを削除する', blog_path(blog.id), method: :delete ,data: { confirm: '本当に削除していいですか？' } %>
      </button>
    </td>
  </tr>
<% end %>
</table>

<br>
